namespace PSO
{
    public enum EFunction { Sphere, Griewangk, Rastrigin, Rosenbock };

    public enum EAlgorithm { Base, Weight, Constriction };

    public enum ETopology { Full, Ring, N4 };
}
